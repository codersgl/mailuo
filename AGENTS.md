# 规则

- 项目需求在`docs/intend.md`中，该文件只能用户修改，但可以给出合理意见。
- 项目规范在`docs/spec.md`中，该文件只能用户修改，但可以给出合理意见。

## 交互规则

- 回答简明扼要。在使用无法避免的术语之前先定义它。
- 提交、issue、PR 评论或代码中不要使用 emoji
- 不要废话或欢快的填充文字（例如，“Thanks @user”而不是“Thanks so much @user!”）
- 对于关键决策需要记录存档
- 将非平凡的设计和问题解释为：问题、具体示例或简短追踪，然后是解决方案。说明该解决方案为什么必要，并把它与可选复杂性区分开。
- 优先使用具体行为和小的示例说明，而不是抽象总结、密集术语或未解释的变更列表。
- 当用户提问时，先回答问题，然后再进行编辑或运行实现命令。
- 回应用户反馈或分析时，先明确说明你同意还是不同意，然后再说明你改了什么。

## 编码规则

- 代码必须有清晰明了的注释
- 在删除看似有意为之的功能或代码之前，始终先询问。
- 除非用户要求，否则不要保留向后兼容性。
- 当重构的价值更高，则考虑重构
- 每完成一个最小功能，调用子代理对代码进行正确性、安全性和质量审核
- 尽可能保持较低的圈复杂度，提高可维护性指数（Maintainability Index）

## 前端规则

- 前端的样式或者布局先用子代理利用HTML制作几个临时的原型供用户参考
- 如果用户采纳其中一版，则基于该版实现，否则根据用户建议进行修改
- 用户定版后，删除临时文件

## 项目规则

- 通过worktree来推进项目
- 只有经过审阅的worktree才能合并到主干
- 每次只推进一小步，完成后先提醒用户验收成果
